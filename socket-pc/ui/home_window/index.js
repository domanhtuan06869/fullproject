const { remote } = require('electron')
remote.getCurrentWindow().openDevTools();
$(document).ready(async()=>{
  // const content =  fs.readFileSync(path.join(__dirname, '../login_window/index.html'), 'utf8');
  // console.log(content);
})